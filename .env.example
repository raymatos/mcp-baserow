# Baserow API Configuration

# Option 1: Username/Password Authentication (Recommended)
# This provides full API access with automatic token refresh
BASEROW_USERNAME=your@email.com
BASEROW_PASSWORD=your_password

# Option 2: Direct JWT Token (Manual)
# JWT tokens expire after 60 minutes
# BASEROW_API_TOKEN=JWT_your_jwt_token_here

# Option 3: Database Token (Limited Scope)
# Database tokens only work with specific databases
# BASEROW_API_TOKEN=Token_your_database_token_here

# Baserow API URL (default: https://api.baserow.io)
BASEROW_API_URL=https://api.baserow.io

# Optional: Default workspace ID to use
# BASEROW_DEFAULT_WORKSPACE_ID=123